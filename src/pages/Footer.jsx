import React from 'react';

const Footer = ({title}) => {
    return (
        <div>
            <hr />
            <h3>footer</h3>
            <p>{title}</p>
        </div>
    );
};

export default Footer;